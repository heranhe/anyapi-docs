# 状态码

// 统一的任务请求状态常量
const (
	TaskRequestStatusSubmitted = "submitted" // 已提交
	TaskRequestStatusError     = "error"     // 错误
	TaskRequestStatusFailed    = "failed"    // 失败
)

// MiniMax任务状态常量
const (
	MinimaxStatusWaiting   = "Waiting"   // 等待中
	MinimaxStatusRunning   = "Running"   // 运行中
	MinimaxStatusSuccess   = "Success"   // 成功
	MinimaxStatusFailed    = "Failed"    // 失败
	MinimaxStatusCancelled = "Cancelled" // 已取消
)
